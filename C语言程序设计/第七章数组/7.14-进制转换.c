#include <stdio.h>
/*输入一个以'#'符为结束标志的字符串(少于10个字符) 滤去所有的非十六进制(不分大小写)
组成一个新的标识十六进制数字的字符串 输出该字符串并将其转换为十进制数后输出
表示十六进制数字的字符为数字字符('0'...'9')、大写英文字母('A'...'F')小写英文字母('a'...'f') 
将十六进制转换为十进制数公式如下：
number = number * 16 +  hexed[i] - '0' hexed[i]数字字符 
number = number * 16 +  hexed[i] - 'A' + 10 hexed[i]大写字符 
number = number * 16 +  hexed[i] - 'a' + 10 hexed[i]小写字符 
*/

int main(void)  {
	int i,
	char str[10];
	return 0;
}
